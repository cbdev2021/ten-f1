import { Box } from "@mui/material";

const Current = () => {
    return (
        <main>
            <Box className="content">
                <h1>Current contain </h1>
            </Box>
        </main>
    );
}

export default Current;
